syntax = "proto3";
package peer;

message SyncMessage {
  bytes data = 1;
}

message Empty{}

service Peer {
  // Sync accepts many sync messages.
  rpc Sync(stream SyncMessage) returns (Empty) { }
}
