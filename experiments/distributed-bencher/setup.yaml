---
- name: Prepare for the experiment
  hosts: all
  tasks:
    - name: Pull etcd docker images
      community.docker.docker_image:
        name: "{{ node_image }}"
        source: pull

    - name: Pull bencher docker images
      community.docker.docker_image:
        name: "{{ bencher_image }}"
        source: pull
