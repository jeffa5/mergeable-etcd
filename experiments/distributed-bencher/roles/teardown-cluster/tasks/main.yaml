- name: Stop etcd docker containers
  docker_container:
    state: absent
    name: "{{ node_name_prefix }}{{ item | int + 1 }}"
    force_kill: true
  with_sequence: start=0 count={{ node_count }}
  when: (item|int % (ansible_play_hosts | length)) == ansible_play_hosts.index(inventory_hostname)
